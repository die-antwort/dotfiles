tabSize = 2
softTabs = true

include = "{$include,.ctags,.dashmate.yml,.env,.eslint*,.gitattributes,.gitignore,.gitkeep,.gitmodules,.htaccess*,.htpasswd,.rubocop*.yml,.ruby-version,.rspec}"
exclude = "{$exclude,*.scssc}"
excludeInFileChooser = "{$exclude,*.{gif,png,jpg,eot,ttf,woff,swf},tmp}"
excludeInFolderSearch = "{$exclude,tmp,log,*.{doc,docx,eot,gif,jpg,JPG,otf,pdf,swf,ttf,woff,xls,xlsx}}"

TM_PHP_OPEN_TAG = ""

windowTitleProject = '${projectDirectory:+ — ${projectDirectory/^.*Projekte\/((P\d{5}[^\/]*|(DA\/[^\/]*))).*/$1/}}'
windowTitle = '${TM_FILEPATH:?${TM_FILEPATH/${projectDirectory}.//}:$TM_DISPLAYNAME}$windowTitleProject$windowTitleSCM'

[ *.tsv ]
tabSize = 8
softTabs = false
