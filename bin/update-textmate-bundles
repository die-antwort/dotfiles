#!/bin/bash

cd ~/Library/Application\ Support/Avian || exit 1

if [ -e Bundles ]; then
  cd Bundles
  git pull -r
  git submodule update
else
  git clone git@github.com:die-antwort/textmate-bundles.git Bundles 
  cd Bundles
  git co textmate2
  git submodule update --init
fi